fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.0
# Current Google SDK. The client also supports the legacy
# `google-generativeai` package if you prefer to pin that instead; installing
# both is unnecessary.
google-genai>=0.3.0
python-dotenv>=1.0.0
requests>=2.31.0
